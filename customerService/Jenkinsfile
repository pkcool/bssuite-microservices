#!/usr/bin/env groovy

node {
    stage('checkout') {
        checkout scm
    }

    stage('check java') {
        sh "java -version"
    }

    stage('clean') {
        sh "cd customerService && ls -a && chmod +x mvnw && ./mvnw clean"
//        sh "ls -a"
//        sh "chmod +x customerService/mvnw"
//        sh "./customerService/mvnw clean"
    }

    stage('backend tests') {
        try {
            sh "cd customerService"
            sh "./mvnw test"
        } catch(err) {
            throw err
        } finally {
            junit '**/target/surefire-reports/TEST-*.xml'
        }
    }

    stage('package and deploy') {
        sh "cd customerService"
        sh "./mvnw com.heroku.sdk:heroku-maven-plugin:1.1.1:deploy-war -DskipTests -Pprod -Dheroku.appName="
        archiveArtifacts artifacts: '**/target/*.war', fingerprint: true
    }

}
